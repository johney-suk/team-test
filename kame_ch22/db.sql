select * from member;

select * from saram;

insert into saram values('KIM','GILDONG',33);

-- JDBC 설정하기
-- 1) DBConnection 드라이버 준비
-- 2) Connection 객체를 만든다.
-- 3) java에서 DB로 쿼리문을 전송한다.
-- 4) 쿼리 실행 결과를 처리한다.